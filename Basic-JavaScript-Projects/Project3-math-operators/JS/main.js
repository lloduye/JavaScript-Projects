function addFunction() { //Defining the function and assigning it a name
    var addition= 12 + 24; //Defining a variablr and assigning it parameters
    document.getElementById("math").innerHTML= "12 + 24 =" + addition;
}